import fs from 'node:fs';import path from 'node:path';const root=path.resolve('dist');if(!fs.existsSync(root)){console.error('dist missing');process.exit(1)}let files=0,max={size:0,file:''};const limit=25*1024*1024;function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else{files++;const s=fs.statSync(p).size;if(s>max.size)max={size:s,file:path.relative(root,p)};if(s>limit){console.error('Asset exceeds 25 MiB Pages limit:',path.relative(root,p),s);process.exitCode=1}}}}walk(root);if(files>20000){console.error('File count exceeds 20000:',files);process.exitCode=1}console.log(JSON.stringify({files,largestAsset:max.file,largestBytes:max.size,perAssetLimitBytes:limit}));
